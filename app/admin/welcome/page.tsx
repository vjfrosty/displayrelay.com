export default function AdminWelcomePage() {
  return (
    <main className="shell">
      <section className="panel">
        <p className="eyebrow">Admin Surface</p>
        <h1 className="title">Welcome scaffolding is in place.</h1>
        <p className="lead">
          Auth, tenant scoping, and the real admin flows land later in Phase 1. For now,
          this route gives the app container a stable placeholder that matches the project
          route plan.
        </p>
      </section>
    </main>
  );
}
